package frc.robot.subsystems.drive.level6;

import frc.robot.subsystems.drive.ModuleIO;

public class Drive extends frc.robot.subsystems.drive.Drive {
  public Drive(ModuleIO flModuleIO, ModuleIO frModuleIO, ModuleIO blModuleIO, ModuleIO brModuleIO) {
    super(flModuleIO, frModuleIO, blModuleIO, brModuleIO);
  }
}
